git pull -> .git -> git local (add/commit) -> .git -> git push


git add   = ajouter un fichier
git commit am- "commentaire"
git push origin master   = mettre en ligne
git pull   = recuperer les donnée
